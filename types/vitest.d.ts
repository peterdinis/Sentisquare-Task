/**
 * Adds custom Jest matchers for asserting on DOM nodes.
 *
 * Extends Jest with additional assertions such as:
 * - toBeInTheDocument()
 * - toHaveTextContent()
 * - toHaveAttribute()
 *
 * This import should be included in your test setup file
 * to enable these matchers globally.
 *
 * @see {@link https://github.com/testing-library/jest-dom}
 */
import "@testing-library/jest-dom";
